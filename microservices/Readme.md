✅ How to use

Build all JARs, Docker images, and start everything:

make all


Only build JARs:

make build


Only build Docker images:

make docker-build


Stop all containers:

make docker-compose-down


Push images to Docker Hub:

make docker-push